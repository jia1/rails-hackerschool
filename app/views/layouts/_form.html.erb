<%= form_for @article do |f| %>
	<p><%= f.label :title %></p>
	<%= f.text_field :title %>
	<p><%= f.label :description %></p>
	<%= f.text_area :description %>
	<p><%= f.submit %></p>
<% end %>

<%= link_to 'Back', :back %>